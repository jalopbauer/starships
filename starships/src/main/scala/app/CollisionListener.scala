package app

import edu.austral.ingsis.starships.ui.{Collision, ElementModel, EventListener, TimePassed}

trait CollisionListener() extends EventListener[Collision]
//case class CollisionListener() extends EventListener[Collision]
